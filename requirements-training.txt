# Week 3 training stack (SPEC_WEEK3.md §1).
# NOTE: on fresh GPU containers (e.g. the Kinesis code-server image) install
# this file together with requirements.txt inside the project venv — see
# docs/week3_kinesis_guide.md §1. datasets>=5 needs the pickling shim that
# training/data.py applies automatically; mT5 trains in bf16 (see config.py).
torch>=2.0
transformers>=4.40
datasets>=2.18
accelerate>=0.28
sentencepiece>=0.1.99
wandb>=0.16
sacrebleu>=2.4
protobuf>=3.20
